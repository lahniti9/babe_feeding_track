import 'package:get/get.dart';

class SpurtBinding extends Bindings {
  @override
  void dependencies() {
    // SpurtController will be created when needed with specific parameters
    // No need to put it here since it requires birthDate and childName
  }
}
